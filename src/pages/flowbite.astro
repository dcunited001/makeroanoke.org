---
import Layout from '../layouts/Base.astro';
import { url, asset } from '../lib/data.ts';

const { title, description } = Astro.props;
---

<Layout title={title} description={description}>
  <h2>Astro</h2>

  Helpers in <code>../lib/data.ts</code>

  <div class="m-6 border border-gray-100">
    <dl class="divide-y">
      <div class="px-4 py-6 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-0">
        <dt class="text-sm font-medium leading-6 text-gray-900">url("test/url")</dt>
        <dd class="mt-1 text-sm leading-6 text-gray-700 sm:col-span-2 sm:mt-0">
          {url("test/url")}
        </dd>
      </div>

      <div class="px-4 py-6 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-0">
        <dt class="text-sm font-medium leading-6 text-gray-900">asset("test/asset.jpg")</dt>
        <dd class="mt-1 text-sm leading-6 text-gray-700 sm:col-span-2 sm:mt-0">
          {asset("test/asset.jpg")}
        </dd>
      </div>
    </dl>
  </div>

  These <code>dl</code> blocks could be refactored into components.

  <h2>Flowbite</h2>

</Layout>
