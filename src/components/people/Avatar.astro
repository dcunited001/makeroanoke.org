---
import { url } from '@/lib/data';
import type { CollectionEntry } from 'astro:content';

type Props = {
  person: CollectionEntry<'people'>;
};

const { person } = Astro.props;
---

<img
  class="w-10 h-10 rounded"
  src={url(person.data.headshot)}
  alt="Default avatar"
/>
