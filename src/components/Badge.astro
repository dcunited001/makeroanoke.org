---
import type { BadgeProps } from '@/components/cva';
import { badgeVariants, cn } from '@/components/cva';

interface Props extends BadgeProps {}

const { variant = 'default', class: className, ...attrs } = Astro.props;
---

<!-- FIXME: typescript typing issues --><!-- <div class:list={cn(badgeVariants({ variant }, attrs.className))} {...attrs}> -->
<div class:list={cn(badgeVariants({ variant }), className)} {...attrs}>
  <slot />
</div>

<!--
<span
  class="me-2 rounded border border-blue-400
  bg-blue-100 px-2.5 py-0.5 text-xs font-medium text-blue-800"
  >Default</span
> -->
